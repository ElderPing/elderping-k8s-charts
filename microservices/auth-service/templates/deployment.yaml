apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: {{ .Values.authService.namespace }}
  labels:
    app: auth-service
spec:
  replicas: {{ .Values.authService.replicas }}
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
        - name: auth-service
          image: "{{ .Values.authService.image.repository }}:{{ .Values.authService.image.tag }}"
          imagePullPolicy: Always
          ports:
            - containerPort: {{ .Values.authService.port }}
          env:
            - name: DB_HOST
              value: {{ .Values.authService.env.dbHost }}
            - name: DB_PORT
              value: {{ .Values.authService.env.dbPort }}
            - name: DB_USER
              value: {{ .Values.authService.env.dbUser }}
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.authService.secrets.dbPasswordSecret }}
                  key: {{ .Values.authService.secrets.dbPasswordKey }}
            - name: DB_NAME
              value: {{ .Values.authService.env.dbName }}
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.authService.secrets.jwtSecretSecret }}
                  key: {{ .Values.authService.secrets.jwtSecretKey }}
            - name: PORT
              value: {{ .Values.authService.env.port }}
          resources:
            requests:
              cpu: {{ .Values.authService.resources.requests.cpu }}
              memory: {{ .Values.authService.resources.requests.memory }}
            limits:
              cpu: {{ .Values.authService.resources.limits.cpu }}
              memory: {{ .Values.authService.resources.limits.memory }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.authService.port }}
            initialDelaySeconds: 15
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.authService.port }}
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
